# ============================================
# YouTube 댓글 자동화 프로그램 설정
# ============================================
# 이 파일을 .env로 복사한 후 값을 채워주세요
# cp .env.example .env

# --- Notion API 설정 ---
# Notion Integration 토큰 (https://www.notion.so/my-integrations 에서 생성)
NOTION_API_TOKEN=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Notion 데이터베이스 ID (URL에서 추출)
# 예: https://www.notion.so/30d35cfd83d38006935dc23424bce256?v=xxx
# → 데이터베이스 ID: 30d35cfd83d38006935dc23424bce256
NOTION_DATABASE_ID=30d35cfd83d38006935dc23424bce256

# --- Notion DB 컬럼명 설정 ---
# 유튜브 링크가 있는 컬럼명
NOTION_COLUMN_YOUTUBE_URL=영상 링크
# 댓글 원고가 있는 컬럼명
NOTION_COLUMN_COMMENT_TEXT=댓글 원고
# 댓글 작성 후 URL을 저장할 컬럼명
NOTION_COLUMN_COMMENT_RESULT_URL=댓글 url
# 작업 상태를 저장할 컬럼명
NOTION_COLUMN_STATUS=상태
# 사용할 계정명 컬럼 (선택)
NOTION_COLUMN_ACCOUNT=댓글 계정

# --- YouTube 계정 설정 ---
# 계정 정보 파일 경로 (JSON)
ACCOUNTS_FILE=config/accounts.json

# --- 프록시 설정 (부계정 IP 관리) ---
# 프록시 사용 여부 (true/false)
USE_PROXY=false
# 프록시 목록 파일 경로
PROXY_LIST_FILE=config/proxies.txt
# 프록시 로테이션 방식: sequential(순차) / random(랜덤)
PROXY_ROTATION=sequential

# --- 브라우저 설정 ---
# 브라우저 헤드리스 모드 (true=화면없이 / false=화면보이기)
HEADLESS=false
# 댓글 작성 후 대기 시간 (초) - 너무 빠르면 봇으로 의심
DELAY_AFTER_COMMENT=5
# 계정 전환 시 대기 시간 (초) - IP 변경 대기
DELAY_AFTER_IP_CHANGE=3
# 페이지 로드 대기 시간 (초)
PAGE_LOAD_TIMEOUT=30

# --- 안전 규칙 설정 (유튜브 바이럴 가이드라인) ---
# 1일 1계정당 최대 댓글 수
# 전략: 1계정당 20개씩 시간 간격을 두고 작업
# 계정 10개 x 20개 = 200개/일 처리 가능
MAX_COMMENTS_PER_DAY=20

# 같은 영상에 다른 계정으로 댓글 달 때 최소 간격 (분)
# 가이드라인: 30~60분 권장
SAME_VIDEO_INTERVAL_MIN=30

# 같은 계정으로 연속 댓글 시 최소 간격 (초)
# 1계정 20개 작업 시 3분 간격 → 약 1시간에 20개 처리
COMMENT_INTERVAL_SEC=180

# --- SMM Kings 좋아요 자동 구매 설정 ---
# SMM 기능 사용 여부 (true/false)
SMM_ENABLED=false

# SMM Kings API 키 (https://smmkings.com → Account → API Key)
SMM_API_KEY=your_api_key_here

# 유튜브 댓글 좋아요 서비스 ID
# 서비스 목록 조회: python -c "from src.smm_client import SMMClient; SMMClient().find_youtube_like_services()"
# 위 명령어로 유튜브 댓글 좋아요 서비스 ID를 확인한 후 입력하세요
SMM_LIKE_SERVICE_ID=4001

# 댓글당 구매할 좋아요 수
SMM_LIKE_QUANTITY=20
